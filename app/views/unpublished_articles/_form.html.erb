<%= form_with(model: unpublished_article, local: true) do |form| %>
  <% if unpublished_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unpublished_article.errors.count, "error") %> prohibited this unpublished_article from being saved:</h2>

      <ul>
      <% unpublished_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_field :comments %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
